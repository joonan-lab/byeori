[project]
name = "byeori"
version = "0.1.0"
description = "A standalone Kiro workspace for curating an OpenAlex-backed literature wiki."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "boto3[crt]>=1.40,<2",
  "httpx>=0.28,<1",
  "mcp>=1.13,<2",
]

[project.scripts]
byeori = "byeori.cli:main"
byeori-mcp = "byeori.mcp_server:main"
byeori-lab-mcp = "byeori.lab_mcp_server:main"

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "pyyaml>=6,<7",
  "cfn-lint>=1.30,<2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/byeori"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
